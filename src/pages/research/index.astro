---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const allResearch = await getCollection('research');
const entries = allResearch
  .filter((r) => r.data.published)
  .sort((a, b) => a.data.priority - b.data.priority);

const statusLabel: Record<string, string> = {
  published: 'Published',
  developing: 'Developing framework',
  manuscript: 'Manuscript in preparation',
};
---
<BaseLayout
  title="Research"
  description="Research publications and developing frameworks by Daniel Edmund Rochatka, spanning AI retrieval, information systems, persistent agents, and foundational physics."
>
  <section class="page-intro section-pad">
    <p class="eyebrow">Research</p>
    <h1>Ideas developed far enough to expose their assumptions.</h1>
    <p>Research here includes formal publications, architecture programs, and manuscripts still being tested. Published and developing work are kept visibly distinct.</p>
  </section>

  <section class="research-list section-pad rule-top">
    {entries.map((entry, i) => (
      <article>
        <div class="research-index">{String(i + 1).padStart(2, '0')}</div>
        <div>
          <p class="research-status">
            {statusLabel[entry.data.status]}
            {entry.data.year ? ` · ${entry.data.year}` : ''}
          </p>
          <h2>
            <a href={`/research/${entry.id}/`}>{entry.data.title}</a>
          </h2>
          <p>{entry.data.summary}</p>
          {entry.data.doiUrl && (
            <a class="text-link" href={entry.data.doiUrl} rel="noopener">
              View on Zenodo <span aria-hidden="true">↗</span>
            </a>
          )}
        </div>
      </article>
    ))}
  </section>
</BaseLayout>
