{
  "name": "daniel-rochatka-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-social-image.mjs && astro build",
    "build:production": "PUBLIC_SITE_ENV=production npm run build",
    "preview": "astro preview",
    "check": "astro check",
    "contact:start": "node server/contact-server.mjs",
    "test:contact": "node --test tests/contact-service.test.mjs",
    "generate:social-image": "node scripts/generate-social-image.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "sharp": "^0.34.5",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
